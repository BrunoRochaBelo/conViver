// Global configuration for the web application
window.APP_CONFIG = {
    API_BASE_URL: 'http://localhost:5000', // Default for local development
    ENABLE_DEBUG_LOGS: false, // Toggle verbose logging in the frontend
    ENABLE_MOBILE_BOTTOM_NAV: true // Show bottom nav on mobile (<=768px)
    // Add other frontend-specific configurations here if needed
};
